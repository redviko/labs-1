#include <iostream>
#include <cmath>
#include <cstdio>

using namespace std;

int main()
{
    double a, b;
    scanf("%lf %lf", &a, &b);
    cout << (a + b) / 2.0 << endl;
    cout << sqrt(a*b) << endl;
    return 0;
}
